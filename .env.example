# Criafy v2 não precisa de nenhuma chave de API.
# Todo o motor de geração roda localmente com o banco de dados de nichos.
# Este arquivo existe só como placeholder — pode não criar .env.local nenhum.

# Opcional: URL pública do seu site (aparece nas copies)
# NEXT_PUBLIC_BASE_URL=https://seu-dominio.com
